# 에이전트 시스템 개발 지침

## 프로젝트 목표

이 레포지토리는 Python과 LangGraph를 사용해 동적 supervisor 기반의 에이전트 시스템을 개발한다. 초기에는 CLI로 실행되는 하나의 프로세스로 운영하지만, 오케스트레이터와 개별 에이전트를 향후 독립 배포할 수 있도록 모듈 사이의 seam을 명확히 유지한다.

## 기본 아키텍처

- 기능 중심 모듈형 모놀리스 구조를 사용한다.
- 호출 흐름은 `CLI → runtime → orchestration → agent interface → agent implementation` 순서를 따른다.
- `cli`는 사용자 입출력만 담당하며 LangGraph 구현에 직접 의존하지 않는다.
- `runtime`은 설정, 모델, persistence, tool registry, agent registry를 조립하는 composition root다.
- `orchestration`은 supervisor의 판단, 라우팅, 정책, 전체 실행 state를 담당한다.
- `orchestration`은 개별 에이전트의 graph, node, prompt를 직접 import하지 않는다.
- 모든 에이전트는 `agents`가 정의하는 공통 인터페이스를 통해 호출한다.
- 설정형 에이전트와 전용 LangGraph 에이전트는 동일한 인터페이스를 만족해야 한다.
- 에이전트는 `tools`, `models`, 공용 `schemas`를 사용할 수 있지만 `orchestration`에 의존하지 않는다.
- 전용 에이전트끼리 서로 직접 import하지 않는다. 협업은 오케스트레이터를 통해 수행한다.
- 오케스트레이터 state와 각 에이전트의 내부 state를 분리한다.
- SQLite와 체크포인트 구현 세부사항은 `persistence` 안에 둔다.
- 실제로 교체되는 구현이 둘 이상일 때 seam과 adapter를 도입한다. 가능성만을 이유로 얕은 추상 계층을 추가하지 않는다.

## 디렉터리 문서 규칙

- 구현 코드를 포함하거나 독립적인 책임을 가진 모든 디렉터리의 최상단에 `MODULE.md`를 둔다.
- 새 모듈 디렉터리를 추가할 때 구현 코드보다 먼저 해당 디렉터리의 `MODULE.md`를 작성한다.
- 파일을 추가·이동·삭제하거나 동작, 인터페이스, 의존성, 데이터 흐름을 변경할 때 관련 `MODULE.md`를 함께 검토하고 갱신한다.
- 하위 `MODULE.md`는 상위 문서와 이 `AGENTS.md`를 따르며, 충돌하지 않는 구체적인 지역 규칙만 추가할 수 있다.
- `MODULE.md`는 단순한 파일 목록이 아니라 코드를 읽기 전에 모듈의 역할과 설계를 이해할 수 있는 문서여야 한다.
- 구현 전인 내용은 현재 상태와 목표 상태를 명확히 구분한다.

각 `MODULE.md`에는 최소한 다음 내용을 포함한다.

1. 목적과 책임
2. 포함할 구현
3. 공개 인터페이스와 사용 방법
4. 의존성과 허용된 import 방향
5. 데이터 및 제어 흐름
6. 설계 결정과 제약사항
7. 테스트 전략
8. 변경 시 문서 갱신 조건

## 작성 언어

- `AGENTS.md`, `MODULE.md`, README, 설계 문서, 운영 문서는 한국어로 작성한다.
- 코드 주석과 docstring도 특별한 이유가 없는 한 한국어로 작성한다.
- Python 식별자, 외부 라이브러리 명칭, 프로토콜 필드, 표준 기술 용어는 원문 표기를 유지할 수 있다.
- 외부에 노출되는 문구가 별도의 언어 요구사항을 가질 때만 해당 요구사항을 우선한다.

## 구현 원칙

- 모듈은 작은 인터페이스 뒤에 충분한 동작을 숨기는 깊은 구조로 설계한다.
- 호출자가 알아야 할 타입, 불변 조건, 오류 형태, 순서 제약을 인터페이스 문서에 명시한다.
- 의존성은 내부에서 생성하지 말고 composition root에서 주입한다.
- 가능한 경우 부수 효과 대신 결과를 반환하고, 외부 I/O는 명확한 seam 뒤에 둔다.
- 공통화는 실제 중복과 변화 지점이 확인된 뒤 수행한다.
- 비밀정보, API 키, 로컬 데이터베이스 파일은 커밋하지 않는다.

## CLI 구현 규칙

- Python CLI의 사용자 출력은 Rich를 사용한다.
- 표, 진행 상태, 상태 메시지, 경고와 오류는 Rich의 구성 요소로 일관되게 표현한다.
- Rich 의존성은 `cli` 모듈 내부에 한정하고, 업무 로직과 오케스트레이션 모듈이 Rich 타입에 의존하지 않도록 한다.
- 자동화와 테스트를 위해 색상 비활성화 및 비대화형 출력이 가능해야 한다.

## 테스트 및 검증

- 모듈의 공개 인터페이스를 주요 테스트 surface로 사용한다.
- 순수한 라우팅 및 정책 로직은 단위 테스트로 검증한다.
- LangGraph의 graph 연결, checkpoint 재개, SQLite 연동은 통합 테스트로 검증한다.
- 외부 모델과 도구 호출은 결정 가능한 fake나 test adapter로 대체한다.
- 변경 후 관련 테스트와 정적 검사를 실행하고, 문서와 실제 구조가 일치하는지 확인한다.
